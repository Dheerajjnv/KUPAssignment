/*pub fn sorting(list: &mut Vec<i32>) -> &mut Vec<i32> {
    let mut temp:i32 = 0;
    for index in 0..list.len()-1{
        for next_index in index+1..list.len(){
            if list[index] > list[next_index] {
                temp = list[index];
                list[index] = list[next_index];
                list[next_index] = temp;
            }
        }
    }
    list

}

pub fn deleting_duplicate(array: &mut Vec<i32>) -> &mut Vec<i32>{
    let store = Vec::new();
}*/
