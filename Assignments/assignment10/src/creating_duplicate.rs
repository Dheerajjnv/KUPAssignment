/*pub fn element_duplication(mut list: Vec<i32>) -> Vec<i32> {
    let mut element_storage: Vec<i32> = Vec::new();
    let mut length = list.len();
    while length != 0 {
        let temp= list.pop()
         match temp{
            Some(temp)=>element_storage.push(temp),
            None=> temp
        };
        element_storage.push(temp);
        element_storage.push(temp);
        length -= 1;
    }
    element_storage
}

*/
